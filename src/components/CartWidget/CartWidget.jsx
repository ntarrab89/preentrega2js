
const CartWidget = ({ children, className = "", onClick }) => {

    return (
        <span className="flex-row">
        <img src='cart.png'/>
        </span>
    );
  };
  
  export default CartWidget;
  